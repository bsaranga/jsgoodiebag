import './index.css';

// The application is composed of a state object(s)
// Read only state tree (immutable)
// State changes are described by actions
// Actions are 'dispatched'
// All data changes are done via actions
// Next state of the application is determined by the previous state and the action

function counter(state, action) {
  return state;
}
